// This file is intentionally minimal.
// All build configuration is in vite.config.ts via @lovable.dev/vite-tanstack-config
// with nitro: { preset: "vercel" } to enable Vercel SSR deployment.
import { defineConfig } from '@tanstack/start/config'

export default defineConfig({})
